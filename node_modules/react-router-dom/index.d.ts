import {Component} from 'react';
export = ReactRouter;
export as namespace Reactrouter;

declare namespace ReactRouter {
  class BrowserRouter<P, S> extends Component<P, S> {  }

  interface RP {
    path: string;
    component: () => JSX.Element;
  }
  class Route<P extends RP, S> extends Component<P, S> {
  }
}